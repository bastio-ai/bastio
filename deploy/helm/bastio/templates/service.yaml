apiVersion: v1
kind: Service
metadata:
  name: {{ include "bastio.fullname" . }}
  labels:
    {{- include "bastio.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bastio.selectorLabels" . | nindent 4 }}
